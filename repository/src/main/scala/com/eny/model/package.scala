package com.eny

import java.util.UUID

package object model {
  type ResponseID = UUID
  type UserID = UUID
  type SocialProfileID = UUID
  type PostID = UUID
  type CategoryID = UUID
  type RatingID = UUID
  type FollowPostID = UUID
  type FollowResponseID = UUID
  type UserDetailsID = UUID
  type ActivityID = UUID
}
